# Screenshots

Add the following screenshots here after running the notebook:

| File | How to get it |
|---|---|
| `ui_single_customer.png` | Run cell 18 → open `http://127.0.0.1:7860` → Tab 1 → fill in fields → screenshot |
| `ui_batch_csv.png` | Tab 2 → upload a slice of `test.csv` → click Run → screenshot the summary |

These images are referenced in the main `README.md` and will display on GitHub automatically once added.
